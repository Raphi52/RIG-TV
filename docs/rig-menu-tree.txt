=== RIG MENU TREE ===
Generated: 2026-06-22 15:55:04

RAIL 1 [RCS]
  SOUSMENU: Administration du RCS : institutionnels, registres, listes, stat
    PROC: Vertical
    PROC: AVINSEE
    PROC: AVISIR_RCS
    PROC: AVISIR
    PROC: BODREJ
    PROC: BORENV
    PROC: BORCOR
    PROC: XXBORENV
    PROC: TRANSBOD
    PROC: CJRECT
    PROC: ACT_IMP
    PROC: JUGE
    PROC: CJNOUV
    PROC: BLRAR
    PROC: BNUMDCA
    PROC: COURRIER
    PROC: COURRIER_D
    PROC: CMD_MINITE
    PROC: NOTIF
    PROC: REGR
    PROC: RETIQ1
    PROC: CFEEMI
    PROC: INPI
    PROC: XXINPI
    PROC: LDG
    PROC: LRAR
    PROC: LRARNI
    PROC: MAND_PC
    PROC: XUSER
    PROC: NPAI
    PROC: CORDJRCS
    PROC: SDCA_PARAM
    PROC: XPARAPH
    PROC: PARA_REG
    PROC: PARA_REDIT
    PROC: PARAPH
    PROC: PARA_DIFF
    PROC: XNUMERO
    PROC: RECHDIR
    PROC: REQUETES
    PROC: XSIGN
    PROC: STFORMA
    PROC: XXSDCA
    PROC: SRETOURNER
    PROC: SURVBOD
    PROC: INPIPAY
    PROC: XXT
    PROC: XXTI
    PROC: VCJNETAT
  SOUSMENU: Autres activites specifiques du registre du commerce et des societes
    PROC: Vertical
    PROC: BOAP
    PROC: XACTION
    PROC: XAC
    PROC: XBCORRI
    PROC: XACORRI
    PROC: BODCP
    PROC: REPRIPMH
    PROC: REPRIPMH2
    PROC: REPRIPPH
    PROC: REPRIPPH2
    PROC: CMD
    PROC: XMENTION
    PROC: MMED
    PROC: NOUVNI
    PROC: MODNI
    PROC: REPRIACH
    PROC: REPRIAC
    PROC: REPRIPP
    PROC: REPRIPP2
    PROC: REPRIPM
    PROC: REPRIPM2
    PROC: CDIBE
    PROC: XXKBIS
    PROC: XXNI
  SOUSMENU: Consultations, informations et visualisations du RCS
    PROC: ATT_AGR
    PROC: ATT_IMMAT
    PROC: VDOSRC
    PROC: VNOTIF
    PROC: RECLAM_REJ
    PROC: REDITE
    PROC: VKREJ
    PROC: VACTION
    PROC: VGED
    PROC: VK
    PROC: VCHIFFRES
    PROC: VREQUI
  SOUSMENU: Depots d'actes de societes, hormis les depots de comptes annuels
    PROC: XDAS
    PROC: XDASNI
    PROC: DAS
    PROC: DASNI
    PROC: DPR
    PROC: RDAS
    PROC: RDASNI
    PROC: XXEXE
    PROC: CSDA
    PROC: CSDANI
    PROC: XDASNO
    PROC: XXDAS
    PROC: XXDASNI
    PROC: VDASNI
  SOUSMENU: Depots de comptes sociaux
    PROC: VDCAREJ
    PROC: XDCA
    PROC: XDPR
    PROC: DCADEMAT
    PROC: DCA
    PROC: RDCA_
    PROC: XDCAEXO
    PROC: SDCA_PARAM
    PROC: XDCANO
    PROC: XDPRNO
    PROC: XXDCA
    PROC: VDCA
  SOUSMENU: Immatriculations au registre du commerce et des societes
    PROC: Vertical
    PROC: LNSIREN
    PROC: IAC
    PROC: I1
    PROC: I1_RMA
    PROC: A1_A
    PROC: A1_C
    PROC: A1_L
    PROC: A1_T
    PROC: A1_TA
    PROC: A1_TL
    PROC: C1
    PROC: D1
    PROC: D1_T
    PROC: B1_A
    PROC: B1_C
    PROC: B1_L
    PROC: B1_T
    PROC: B1_TA
    PROC: B1_TL
    PROC: I2
    PROC: A2
    PROC: C2
    PROC: D2
    PROC: B2
  SOUSMENU: Modifications au registre du commerce et des societes
    PROC: Vertical
    PROC: MA1
    PROC: MA2
    PROC: MB1
    PROC: MB2
    PROC: MC1
    PROC: MC2
    PROC: MD2
    PROC: MI1
    PROC: MI2
    PROC: MAC
    PROC: MD1
    PROC: MOF
    PROC: MOF_PC
    PROC: MMD
    PROC: MY
    PROC: MC1C2
    PROC: MD1D2
    PROC: MD2D1
    PROC: MD1B2
    PROC: MA1A2
    PROC: MA2A1
    PROC: MB2B1
    PROC: MC2C1
    PROC: MC1D1
    PROC: MC1D2
    PROC: MC1B2
    PROC: MC1B1
    PROC: MC2D1
    PROC: MC2D2
    PROC: MC2B1
    PROC: MC2B2
    PROC: MB1B2
    PROC: MD1C1
    PROC: MD1C2
    PROC: MD1B1
    PROC: MD2C1
    PROC: MD2C2
    PROC: MD2B2
    PROC: MD2B1
    PROC: MB2D2
    PROC: MB1C1
    PROC: MB1C2
    PROC: MB1D1
    PROC: MB1D2
    PROC: MB2C1
    PROC: MB2C2
    PROC: MB2D1
  SOUSMENU: Preparation XML RCS
    PROC: TRCJNRET
  SOUSMENU: Radiations au registre du commerce et des societes
    PROC: RY
    PROC: RA1
    PROC: RI2
    PROC: RA2
    PROC: RB1
    PROC: RB2
    PROC: RC1
    PROC: RC2
    PROC: RD1
    PROC: RD2
    PROC: RADAC
    PROC: RI1
    PROC: ROF
  SOUSMENU: Surveillances internes du RCS et relances
    PROC: Vertical
    PROC: SURV
    PROC: LACTION
    PROC: GCONV
    PROC: ISURV
    PROC: MD
    PROC: MSDCA
    PROC: MDIL
    PROC: PARAM_INJ
    PROC: SURV_PARAM
    PROC: PARAM_NAT_DIL
    PROC: PARAM_SURV
    PROC: PREV_GES
    PROC: PREVENTION
    PROC: ROFSURV
    PROC: RSURV
    PROC: RNSURVDEMAT
    PROC: RSIG
    PROC: RNSURV
    PROC: XX_ORD
    PROC: S123-130
    PROC: S123-129-2
    PROC: SURVSCT
    PROC: SURVMD
    PROC: S123-131
    PROC: S123-171
    PROC: S123-136
    PROC: SASSOSCI
    PROC: SSAC2A
    PROC: VDIL
    PROC: VSURV
    PROC: VSDCA
  SOUSMENU: Preparation XML ADM
    PROC: EDITXMLEQU
    PROC: EDITXMLREF
  SOUSMENU: Organisation du greffe
    PROC: ARCHIRAD
    PROC: EDT_DIFF
    PROC: CTRLACT
    PROC: XENTREP
    PROC: RCS_DISS
    PROC: TABLREF
    PROC: VPROSS
    PROC: GFEDERR
    PROC: LNUMCM
    PROC: XSERVICE
    PROC: XDEMANDE
    PROC: MDEMANDE
    PROC: RCS_RAPP
    PROC: STATCNG
    PROC: XPILOTAGE

RAIL 2 [ENDETTEMENT]
  SOUSMENU: Administration de l'endettement, registres, listes, stat
    PROC: CREANREP
    PROC: CNAV
    PROC: XINSH
    PROC: XCREANREP
    PROC: STATNANTI
    PROC: XXINS
    PROC: REGE
    PROC: TRA_INS
  SOUSMENU: Consultations, informations, visualisations de l'endettement
    PROC: LET_BAT
    PROC: VINS
    PROC: RRME
    PROC: RECH_NAV
  SOUSMENU: Inscriptions en matiere d'endettement (niveau activite)
    PROC: XINS
    PROC: INSBAT
    PROC: INS_HF
    PROC: INS_M
    PROC: REN
    PROC: CINS
    PROC: CINSM
    PROC: RENTP
    PROC: INS_S
  SOUSMENU: Mentions marginales en matiere d'endettement
    PROC: INS_NJD
    PROC: MMA
  SOUSMENU: Radiations d'inscriptions d'endettement
    PROC: RAD_INS
  SOUSMENU: Specialites endettement
    (vide)
  SOUSMENU: Surveillances internes du domaine de l'endettement
    (vide)

RAIL 3 [AFFAIRES JUDICIAIRES]
  SOUSMENU: Enrolements ou inscriptions au repertoire general des affaires
    PROC: NI
    PROC: REPRIPCH
    PROC: NPC
    PROC: NF
    PROC: EJC
    PROC: CLOT
    PROC: INJDCA
    PROC: INJDCA_
    PROC: CJUD
    PROC: EOUV
    PROC: EF
    PROC: REPRIAFFH
    PROC: EDCP
    PROC: EDS
    PROC: OPE
    PROC: IPE
  SOUSMENU: Modifications affaires ou procedures collectives
    PROC: DEPPCM
    PROC: MPC
    PROC: MPCS
    PROC: IPSUIVI
    PROC: PARTIES
    PROC: MJUDS
    PROC: MJUD
    PROC: DEPPC
    PROC: FCOM
    PROC: SIGNJUD
    PROC: ORDOSIGN
    PROC: JOIN
    PROC: MESIN
  SOUSMENU: Jugement (Redaction, editions)
    PROC: IMPLEG
    PROC: REDAA
    PROC: REDADS
    PROC: REDAG
    PROC: REDAM
    PROC: REDAX
    PROC: REDAS
    PROC: REDA
    PROC: REDA_MASSE
  SOUSMENU: Audiences et cabinets
    PROC: GESTAUDS
    PROC: GESTCABS
    PROC: AUDIENCE
    PROC: COMPOM
    PROC: GESTAUD
    PROC: GESTCAB
    PROC: COMPO
    PROC: PRECAB
    PROC: PREAUDPC
    PROC: PREAUD
    PROC: RETAUD
    PROC: RETCAB
  SOUSMENU: EDI judiciaires
    PROC: RPVAINS
    PROC: DCOFFRE
    PROC: COFFREFORT
    PROC: RPVAREN
    PROC: ENVBOD
    PROC: ENVJAL
    PROC: GESTBOD
    PROC: VNOTIF
    PROC: ETA_WEB
    PROC: ETAINFOG
    PROC: VFNIG
    PROC: SUIVICENT
    PROC: FNIG
    PROC: SURVMOF
    PROC: IPEDEMAT
    PROC: DOC_TRIDIG
    PROC: RPVACONC
    PROC: VTRID
  SOUSMENU: Consultations, statistiques, surveillance, editions en masse
    PROC: VHISTOPC
    PROC: REGJUD
    PROC: JREG
    PROC: CLOT
    PROC: LSTDELIB
    PROC: LISTEEXCEL
    PROC: PAPERCUT
    PROC: STATCA
    PROC: STATCNG
    PROC: STATINTERNES
    PROC: STAT
    PROC: SURVIP
    PROC: SURVMES
    PROC: SURVPC
    PROC: TABBORD
    PROC: VJUD
  SOUSMENU: Acteurs judiciaires
    PROC: CHJCM
    PROC: CHMANDM
    PROC: JUGE
    PROC: GEXPERT
    PROC: GJUGE
    PROC: GDESTINST
    PROC: GGREFFIERS
    PROC: GINTERV
    PROC: GMANDPC
    PROC: GPARQU
    PROC: GREPER
    PROC: MANDAT_CLI
    PROC: MANDATAIRE
    PROC: MAND_PC
    PROC: MODIFMAND
    PROC: MODIFREPER
    PROC: RAPPROREP
    PROC: RAPPACTEUR
    PROC: RAPPACTEUR_
  SOUSMENU: Outils divers
    PROC: AFFDONNEESTGI
    PROC: ADDCREA
    PROC: RETOURLRAR_
    PROC: TAXESETTOURS
    PROC: CORRESPMIG
    PROC: CORRESPGAGI
    PROC: CORRESPORACLE
    PROC: GEDJUD
    PROC: INTCREA
    PROC: LSTCEPMAQ
    PROC: MODCREA
    PROC: RECHTABREF
    PROC: RECHUTITABREF
    PROC: VAX
    PROC: RETOURLRAR
  SOUSMENU: Parametrages divers
    PROC: PARAMIP_
    PROC: PARAMIP
    PROC: PARAMMES
    PROC: PARAMPROC
    PROC: PARAMJUD
  SOUSMENU: Editions particulieres ou en masse
    PROC: JLR
    PROC: BORDLR
    PROC: EDITM
    PROC: COURRIERM
    PROC: LISTEINS
    PROC: LISTEPC
  SOUSMENU: Suppressions et correction affaires ou procedure
    PROC: XAFF
    PROC: XPC
    PROC: XLIENPC
    PROC: XXMESURE
    PROC: XXAFF
    PROC: XXPC
    PROC: MDJUD

RAIL 4 [PREVENTION]
  SOUSMENU: Enrolement de prevention
    PROC: NPR
    PROC: EMC
    PROC: EPR
  SOUSMENU: Modification de prevention
    PROC: XLIENPR
    PROC: MODIPR
    PROC: MODPR
    PROC: MPR
    PROC: DEPPR
    PROC: PRCOM
    PROC: JOINPR
  SOUSMENU: Jugement (redaction, editions)
    PROC: IMPLEG
    PROC: REDAA
    PROC: REDADS
    PROC: REDAG
    PROC: REDAM
    PROC: REDAX
    PROC: REDAS
    PROC: REDAPR
  SOUSMENU: Consultations, statistiques et surveillances
    PROC: LISTEEXCEL
    PROC: PREVENTION
    PROC: LISTEPR
    PROC: STATCNG
    PROC: SURVENTPR
    PROC: SURVPR
    PROC: TABBORD
    PROC: VJUDPR
  SOUSMENU: Suppressions et corrections preventions
    PROC: XAFFPR
    PROC: XXAFFPR
    PROC: XXPRV
    PROC: MDJUDPR

RAIL 5 [COMPTABILITE]
  SOUSMENU: Gestion commerciale
    PROC: GCOM
    PROC: GESTCOMM
    PROC: JCAISSELR
    PROC: CLIPROVILR
    PROC: CLIRELEVERAPIDELR
    PROC: SOLDEMULTIFACT
  SOUSMENU: Trait. journaliers
    PROC: FIGER
    PROC: FIGER_EXP
    PROC: JCAISSE
    PROC: JVENTE
    PROC: RECHENC
    PROC: REMBANQ
    PROC: REMISBQ
    PROC: SUIVIENCAISS
  SOUSMENU: Procedures collectives
    PROC: PAYEURAFFECT
    PROC: RELCPTEPC
    PROC: CPTEMANDATAIRE
    PROC: PCETAT
    PROC: PAYEURCHANGE
    PROC: JONCTIONPC
    PROC: PCORDTRESOR
    PROC: PCENCAISS
    PROC: FACTPCMASSEACTE
    PROC: FACTPCMASSE
    PROC: PCREPART
  SOUSMENU: Preventions
    PROC: PAYEURAFFECTPREV
    PROC: RELCPTEPREV
    PROC: PREVETAT
    PROC: PAYEURCHANGEPREV
    PROC: PREVREPART
  SOUSMENU: Gestion Clients
    PROC: CLIBALANCE
    PROC: CLILISTE
    PROC: CLIETIQUETTE
    PROC: CLIDIVERS
    PROC: CLIGESTION
    PROC: CLIPROVI
    PROC: GCLIENT
    PROC: RELCLI
    PROC: CLIRELEVE
    PROC: CLIRELEVERAPIDE
  SOUSMENU: Gestion Tarifs
    PROC: ARTCAT
    PROC: ARTEDITTARIF
    PROC: ARTGROUPE
    PROC: ARTGESTION
    PROC: ARTGESTION2
    PROC: TARIFS
    PROC: ARTTVA
  SOUSMENU: Lettres cheques
    PROC: TROPPERCU
    PROC: LCCLTCPTE
    PROC: EDNOTIF
    PROC: EDNOTIFEDI
    PROC: LCGROUPEES
    PROC: EDCHQEMIS
    PROC: VCHEQEMI
    PROC: ENCPERDUS
  SOUSMENU: Internet
    PROC: CLIBALANCEW
    PROC: CLILISTEW
    PROC: CLIEVOLSOLDEW
    PROC: CLIGESTIONW
    PROC: CLIPROVIW
    PROC: CLIRELEVEW
    PROC: CLIRELEVERAPIDEW
  SOUSMENU: Traitements periodiques
    PROC: CENTRAVOC
    PROC: NONUTIL
    PROC: RELFACIMP
    PROC: JTVA
    PROC: JVENTEP
    PROC: ENCREJET
  SOUSMENU: Expertises
    PROC: EXPERTBALANCE
    PROC: EXPERTERB
    PROC: GESTBANQEXP
    PROC: EXPERTCOMPTES
    PROC: EXPERTTRANSFERT
  SOUSMENU: Administration
    PROC: GBANQUE
    PROC: GESTBANQ
    PROC: GESTDROITSEXP
    PROC: GESTMODEPAIEMENT
    PROC: GPAIEMENT
    PROC: PAYEURLISTE
    PROC: GPAYEUR
    PROC: LIBAFFAIRE
    PROC: LIBEREAFFAIRE
    PROC: PARAMEXPRESSO
    PROC: PARAM
    PROC: REGENLIGNE
    PROC: STAT_EXP
  SOUSMENU: Outils divers
    PROC: GINPI
    PROC: INTCREANCE
    PROC: EDIFIBEN
    PROC: RELVEBOD
    PROC: STATINTERNES
  SOUSMENU: Comptabilite
    PROC: CLOTJOURNAUX
    PROC: EXPRESSO
    PROC: COMPTA_EDIT
    PROC: ETAT_RAPPROCHEMENT
    PROC: COMPTA_IMPORT
    PROC: LCFOURNISSEUR
    PROC: RECHECRITURE
    PROC: COMPTA_ECRITURES
    PROC: PASSAGE_COMPTA
  SOUSMENU: Parametrages
    PROC: COMPTA_EXERCICE
    PROC: COMPTA_FOURN
    PROC: COMPTA_MODELE
    PROC: COMPTA_JOURNAUX
    PROC: CPTPLAN
  SOUSMENU: Traitements journaliers
    PROC: FACT

RAIL 6 [GENERAL]
  SOUSMENU: Outils
    PROC: Vertical
    PROC: CNXDIST
    PROC: CTRLACT
    PROC: COURRIER
    PROC: COURRIER_D
    PROC: VPROSS
    PROC: EDITRCS
    PROC: GESTIMPRJUD
    PROC: COURRIER_DIVERS
    PROC: GESTDATE
    PROC: GESTLOT
    PROC: RELANCEFILE
    PROC: RELANCELOT
    PROC: GESTMENU
    PROC: GESTPROC
    PROC: GESTROLE
    PROC: GESTUTIL
    PROC: GESTEMAIL
    PROC: NEWS
    PROC: LNUMCM
    PROC: XDEMANDE
    PROC: MDEMANDE
    PROC: LISTES
    PROC: POOL_EDIT
    PROC: NUM
    PROC: SUIVI_IMPRESSION
  SOUSMENU: Parametrages
    PROC: RIGADMIN
    PROC: TABLREF
    PROC: GTABREF
    PROC: XSERVICE
    PROC: XUSER
    PROC: MAQUETTE
    PROC: PARAMJUD
    PROC: PARAMACC
    PROC: TABLEREF
  SOUSMENU: Statistiques
    PROC: LISTEEXCEL
    PROC: STATCA
    PROC: STATNANTI
    PROC: STATCNG
    PROC: STAT
    PROC: STFORMA
    PROC: TABBORD
    PROC: XPILOTAGE
  SOUSMENU: Accueil, Requisition
    PROC: REQ
    PROC: XEX
    PROC: XPARAPH
    PROC: PARA_REG
    PROC: PARA_REDIT
    PROC: PARAPH
    PROC: PARA_DIFF
    PROC: RECHDIR
    PROC: REQUI
    PROC: ARRIVEE
  SOUSMENU: Modification d'entreprises
    PROC: XENTREP
    PROC: RCS_DISS
    PROC: RCS_RAPP
  SOUSMENU: Casier judiciaire
    PROC: CJRECT
    PROC: CJNOUV
    PROC: VCJNETAT

RAIL 7 [(absent)]
=== TOTAUX : 6 rails, 56 sous-menus, 603 processus ===
