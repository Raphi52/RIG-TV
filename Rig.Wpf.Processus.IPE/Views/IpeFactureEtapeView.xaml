<UserControl x:Class="Rig.Wpf.Processus.Ipe.Views.IpeFactureEtapeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:etapes="clr-namespace:Rig.Wpf.Processus.Ipe.Etapes"
             d:DataContext="{d:DesignInstance etapes:IpeFactureEtapeViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160" />
            <ColumnDefinition Width="*" MaxWidth="320" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.ColumnSpan="2" Text="{Binding Libelle}"
                   FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Montant (€)"
                   VerticalAlignment="Center" Margin="0,4" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,4"
                 Text="{Binding Montant, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}" />
    </Grid>
</UserControl>
