<UserControl x:Class="Rig.Wpf.Processus.Ipe.Views.IpeDemandeEtapeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:etapes="clr-namespace:Rig.Wpf.Processus.Ipe.Etapes"
             d:DataContext="{d:DesignInstance etapes:IpeDemandeEtapeViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160" />
            <ColumnDefinition Width="*" MaxWidth="320" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.ColumnSpan="2" Text="{Binding Libelle}"
                   FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Type d'IP"
                   VerticalAlignment="Center" Margin="0,4" />
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,4"
                  ItemsSource="{Binding TypesIpDisponibles}"
                  SelectedItem="{Binding TypeIp, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Date saisine greffe"
                   VerticalAlignment="Center" Margin="0,4" />
        <DatePicker Grid.Row="2" Grid.Column="1" Margin="0,4"
                    SelectedDate="{Binding DateSaisineGreffe, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>
