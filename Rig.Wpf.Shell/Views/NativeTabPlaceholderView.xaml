<UserControl x:Class="Rig.Wpf.Shell.Views.NativeTabPlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#FAFAFA">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding CodeProcessus}" FontSize="20" FontWeight="Bold"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Processus WPF natif (Sprint 2 — pilote PROC_TESTNLH)"
                       Foreground="#666" Margin="0,8,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
