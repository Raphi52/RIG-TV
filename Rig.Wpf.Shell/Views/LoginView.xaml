<UserControl x:Class="Rig.Wpf.Shell.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Rig.Wpf.Shell.ViewModels"
             d:DataContext="{d:DesignInstance vm:LoginViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="320" Margin="16">
        <TextBlock Text="Connexion" FontSize="20" FontWeight="Bold" Margin="0,0,0,16" />

        <TextBlock Text="Code utilisateur" />
        <TextBox Margin="0,4,0,12"
                 Text="{Binding CodeUtilisateur, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Code greffe" />
        <TextBox Margin="0,4,0,12"
                 Text="{Binding CodeGreffe, UpdateSourceTrigger=PropertyChanged}" />

        <Button Content="Se connecter" Padding="12,6" HorizontalAlignment="Right"
                Command="{Binding LoginCommand}" />
    </StackPanel>
</UserControl>
