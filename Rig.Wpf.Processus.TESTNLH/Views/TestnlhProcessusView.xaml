<UserControl x:Class="Rig.Wpf.Processus.Testnlh.Views.TestnlhProcessusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Rig.Wpf.Processus.Testnlh"
             xmlns:etapes="clr-namespace:Rig.Wpf.Processus.Testnlh.Etapes"
             xmlns:views="clr-namespace:Rig.Wpf.Processus.Testnlh.Views"
             d:DataContext="{d:DesignInstance vm:TestnlhProcessusViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type etapes:TestnlhEtape1ViewModel}">
            <views:TestnlhEtape1View />
        </DataTemplate>
    </UserControl.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#F5F5F5" Padding="16,8">
            <TextBlock Text="{Binding Libelle}" FontSize="18" FontWeight="Bold" />
        </Border>

        <Border DockPanel.Dock="Bottom" Background="#EEEEEE" Padding="16,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="{Binding LastSavedMessage,
                                  StringFormat='Dernier sauvegardé : {0}',
                                  TargetNullValue='(aucun)',
                                  FallbackValue='(aucun)'}"
                           Margin="0,0,16,0" VerticalAlignment="Center"
                           Foreground="#555" />
                <Button x:Name="SaveButton" Content="Enregistrer" Padding="12,4"
                        Command="{Binding SaveCommand}" />
            </StackPanel>
        </Border>

        <ContentControl Content="{Binding CurrentEtape}" />
    </DockPanel>
</UserControl>
