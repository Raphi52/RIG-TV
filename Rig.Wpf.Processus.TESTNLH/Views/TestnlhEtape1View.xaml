<UserControl x:Class="Rig.Wpf.Processus.Testnlh.Views.TestnlhEtape1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:etapes="clr-namespace:Rig.Wpf.Processus.Testnlh.Etapes"
             d:DataContext="{d:DesignInstance etapes:TestnlhEtape1ViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel Margin="16">
        <TextBlock Text="{Binding Libelle}" FontSize="16" FontWeight="SemiBold"
                   Margin="0,0,0,12" />

        <TextBlock Text="Message de test" />
        <TextBox x:Name="MessageBox"
                 Margin="0,4,0,12"
                 Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Foreground="#888"
                   Text="{Binding IsValid, StringFormat='Valide : {0}'}" />
    </StackPanel>
</UserControl>
